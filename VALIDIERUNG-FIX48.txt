LiMaD OS 2.7.0 RC1 – FIX48 Validierungsplan

Aktive Marker:
- gnome42-phase4-fix48
- 2.7.0-rc1-fix48
- default-flatpaks-fix48.done
- plymouth-initramfs-fix48.done

Geprüfte Reparatur:
- limad-plymouth-initramfs: OSTree-Boot-Erkennung von "[[ -d /run/ostree-booted ]]"
  auf "[[ -e /run/ostree-booted ]]" korrigiert
- neuer Regressionstest verbietet den fehlerhaften -d-Test dauerhaft
- alle übrigen FIX47-Reparaturen (Klang, Installer-Branding, GitHub-Upload)
  unverändert und weiterhin geprüft

Pflichtprüfungen vor Freigabe:
1. tests/validate.sh mit Pillow
2. tests/validate.sh mit LIMAD_TEST_FORCE_NO_PIL=1
3. Shell-, Python-, JSON- und Cache-Audit
4. fertiges ZIP entpacken und tests/validate.sh aus einer frischen Kopie starten
5. ZIP-Integrität und SHA-256

Grenze der Offline-Prüfung:
Der sichtbare Bootscreen auf echter Hardware kann nur ein tatsächlicher
Neustart des installierten Systems bestätigen. Offline lässt sich nur
verifizieren, dass der blockierende Guard nicht mehr fälschlich greift und
die anschließenden rpm-ostree-Befehle im Quelltext tatsächlich erreicht
werden.
