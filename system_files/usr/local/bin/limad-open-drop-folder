#!/usr/bin/env bash
set -Eeuo pipefail
/usr/local/bin/limad-user-folders-setup
folder="$(xdg-user-dir DOWNLOAD 2>/dev/null || printf '%s/Downloads' "$HOME")/LiDrop"
mkdir -p "$folder"
exec xdg-open "$folder"
